/**
 * Session key hook — thin wrapper around InterwovenKit's autoSign.
 * Exposes a simplified interface for the session modal.
 * Use useInterwovenKit() directly for full autoSign control.
 */
export { useInterwovenKit as useSessionKey } from '@initia/interwovenkit-react'
