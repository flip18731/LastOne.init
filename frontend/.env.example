# Chain Configuration — update after appchain deployment
VITE_CHAIN_ID=lastone-1
VITE_RPC_URL=https://rpc.lastone.init.testnet
VITE_REST_URL=https://rest.lastone.init.testnet

# Contract — update after deployment
VITE_CONTRACT_ADDRESS=init1...

# AI Commentator — Anthropic API key
# WARNING: In production, proxy through a backend to protect the key
VITE_ANTHROPIC_API_KEY=sk-ant-...

# Demo mode — uses mock data when contract is not deployed
VITE_USE_MOCK=true
